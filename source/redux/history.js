import createHistory from "history/createHashHistory";

// History supports several modes. Use "createBrowserHistory" for proper path-based
// routing, perhaps in a server environment.

export default createHistory();
